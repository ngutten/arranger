# Requirements for scripts/train_ddsp.py
# Install into a separate venv — these are NOT required by the main app.
#
#   python -m venv .venv-ddsp
#   source .venv-ddsp/bin/activate
#   pip install -r scripts/requirements-ddsp.txt

torch>=2.0
torchaudio>=2.0
onnx>=1.14
librosa>=0.10
soundfile>=0.12
numpy>=1.24
tqdm
